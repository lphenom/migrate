services:
  php:
    build:
      context: .
      dockerfile: Dockerfile
      target: dev
    volumes:
      - .:/app
    working_dir: /app
    environment:
      - APP_ENV=development
    stdin_open: true
    tty: true
    command: ["tail", "-f", "/dev/null"]
